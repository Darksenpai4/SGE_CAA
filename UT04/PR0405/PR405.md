# PR404

## Ejercicio 1

### Filtrado de una lista de números:

Usa filter para obtener solo los números pares de una lista de enteros.

```python
# Ejemplo de lista de entrada
celsius = [0, 20, 37, 100]
# Resultado esperado: [32.0, 68.0, 98.6, 212.0]
```

Ejercicio:

```python
numeros = [8, 29, 6, 4, 0, 1, 28, 91, 23, 9, 11, 77, 99, 7, 42]

pares = (list(filter(lambda x: x%2 == 0, numeros)))

print(pares)
```

## Ejercicio 2

### Mapeo de temperaturas

Dada una lista de temperaturas en Celsius, usa map para convertirlas a Fahrenheit.

```python
# Ejemplo de lista de entrada
celsius = [0, 20, 37, 100]
# Resultado esperado: [32.0, 68.0, 98.6, 212.0]
```

Ejercicio:

```python

temper = [8, 29, 6, 4, 0, 1, 28, 91, 23, 9, 11, 77, 99, 7, 42]

fahrenheit = list(map(lambda x: (x * 9/5 )+ 32, temper))

print(fahrenheit)

```

## Ejercicio 3

### Suma acumulativa

Utiliza reduce para obtener la suma acumulativa de una lista de números.

```python
# Ejemplo de lista de entrada
numeros = [1, 2, 3, 4, 5]
# Resultado esperado: 15
```

Ejercicio:
```python
from functools import reduce

numeros = [8, 29, 6, 4, 0, 1, 28, 91, 23, 9, 11, 77, 99, 7, 42]
# acc: valor acumulado en este elemento
# 0: valor inicial del elemento
# val: valor de cada uno de los iterables del elemento números. sería un for(int val : numeros){ suma = val + acc}
suma = reduce(lambda acc, val: acc + val, numeros, 0)
print(suma)
```

## Ejercicio 4

### Palabras con cierta longitud

Dada una lista de palabras, usa filter para encontrar las que tienen más de cinco letras y luego map para convertirlas en mayúsculas.

```python
# Ejemplo de lista de entrada
palabras = ["perro", "gato", "elefante", "oso", "jirafa"]
# Resultado esperado: ['ELEFANTE', 'JIRAFA']
```

Ejercicio:

```python
lista = ["pomelo", "pesa", "perfume", "guitarra", "dinero", "cosa", "luna"]

mayorCinco = (list(filter(lambda x: len(x) > 5, lista)))
mayusculas = list(map(lambda x: x.upper(), mayorCinco))

print(mayusculas)
```

## Ejercicio 5

### Multiplicación de pares

Dada una lista de números, utiliza filter, map y reduce para obtener el producto de los números pares.

```python
# Ejemplo de lista de entrada
numeros = [1, 2, 3, 4, 5, 6]
# Resultado esperado: 48 (producto de 2, 4 y 6)
```

Ejercicio:

```python
from functools import reduce
numeros = [8, 29, 6, 4, 1, 28, 91]

pares = (list(filter(lambda x: x % 2 == 0, numeros)))
producto = reduce(lambda prod, num: prod * num, pares, 1)

print("Números pares a multiplicar: ", pares)
print("Resultado", producto)
```

## Ejercicio 6

### Combinar operaciones en listas anidadas

Dada una lista de listas de enteros, usa map, filter, y reduce para obtener la suma de todos los números positivos.

```python
# Ejemplo de lista de entrada
numeros = [[-3, 2, 7], [10, -5, 3], [0, 8, -2]]
# Resultado esperado: 30 (suma de 2, 7, 10, 3, y 8)
```

Ejercicio:

```python
from functools import reduce
numeros = [
    [8, -29, 1],
    [4, -46, 6],
    [-11, 7, -91]
]

positivos = list(map(lambda sublista: list(filter(lambda x: x > 0, sublista)), numeros))
sumaTotal = reduce(lambda acc, sublista: acc + sum(sublista), positivos, 0)

print("Suma de todos los números positivos:", sumaTotal)
```

## Ejercicio 7

### Frecuencia de palabras

Dado un texto, crea una función que use map y reduce para obtener la frecuencia de cada palabra. Ignora las mayúsculas y minúsculas y supón que no hay símbolos de puntuación.

```python
# Ejemplo de texto de entrada
texto = "Hola hola mundo mundo cruel"
# Resultado esperado: {'hola': 2, 'mundo': 2, 'cruel': 1}
```

Ejercicio:

```python
from functools import reduce
texto = "Este es un texto de ejemplo este texto es solo un ejemplo"

def frecuencia_palabras(t):
    palabras = list(map(lambda x: x.lower(), t.split()))
    
    # Función reductora que acumula las frecuencias en un diccionario
    def acumulador(freq, palabra):
        freq[palabra] = freq.get(palabra, 0) + 1
        return freq
    
    return reduce(acumulador, palabras, {})

resultado = frecuencia_palabras(texto)
print(resultado)
```

## Ejercicio 8

### Intersección de conjuntos

Dadas dos listas de números, usa filter para obtener una lista de los elementos que están en ambas listas (sin usar conjuntos).

```python
# Ejemplo de listas de entrada
lista1 = [1, 2, 3, 4, 5]
lista2 = [3, 4, 5, 6, 7]
# Resultado esperado: [3, 4, 5]
```

Ejercicio:

```python
num1 = [8, 29, 1, 4, 0, 6]
num2 = [28, 6, 91, 4, 11]

resultado = (list(filter(lambda x: x in num2, num1)))

print("Elementos en ambas listas:", resultado)
```

## Ejercicio 9

### Agrupación de palabras por longitud

Dada una lista de palabras, crea un diccionario donde la clave es la longitud de la palabra y el valor es una lista de palabras de esa longitud. Usa map y filter.

```python
# Ejemplo de lista de entrada
palabras = ["sol", "luna", "estrella", "cielo", "mar"]
# Resultado esperado: {3: ['sol', 'mar'], 4: ['luna'], 5: ['cielo'], 8: ['estrella']}
```

Ejercicio:

```python
lista = ["pomelo", "pesa", "perfume", "guitarra", "dinero", "cosa", "luna"]

diccionario = {}
mayorLong = 0
longitudes = set(map(lambda x: len(x), lista))
for longitud1 in longitudes:
    if mayorLong < longitud1:
        mayorLong = longitud1
        
for i in sorted(longitudes):
    diccionario[i] = list(filter(lambda x: len(x) == i, lista))
    

print(diccionario)
```

## Ejercicio 10

### Concatenación de listas de caracteres

Dadas dos listas de caracteres, usa reduce para concatenarlas en una sola lista sin utilizar + o métodos de concatenación.

```python
# Ejemplo de listas de entrada
lista1 = ['a', 'b', 'c']
lista2 = ['x', 'y', 'z']
# Resultado esperado: ['a', 'b', 'c', 'x', 'y', 'z']
```

Ejercicio:

```python
from functools import reduce
x = ["a", "b", "c"]
y = ["d", "e", "f"]

x = reduce(lambda a, b: a + [b], y, x)

print(x)
```

## Ejercicio 11

### Calificación de alumnos

Dada una lista de tuplas con el nombre del alumno y su calificación, utiliza map y filter para obtener una lista con los nombres de los alumnos que han aprobado (nota >= 5).

```python
# Ejemplo de lista de entrada
alumnos = [("Ana", 4), ("Bruno", 7), ("Clara", 5), ("David", 8)]
# Resultado esperado: ['Bruno', 'Clara', 'David']
```

Ejercicio:

```python
alumnos = [("Mia", 4), ("Pablo", 1), ("Marizza", 6), ("Manuel", 8)]

alumnos = list(map(lambda x: x[0], filter(lambda y: y[1] >= 5, alumnos)))

print("Alumnos aprobados: ", alumnos)
```

## Ejercicio 12

### Calcular producto cartesiano

Dadas dos listas de números, usa map para obtener el producto cartesiano de ambas listas, devolviendo una lista de tuplas.

```python
# Ejemplo de listas de entrada
lista1 = [1, 2]
lista2 = [3, 4]
# Resultado esperado: [(1, 3), (1, 4), (2, 3), (2, 4)]
```

Ejercicio:

```python
lista1 = [1, 2]
lista2 = [3, 4]

listaNueva = list(map(lambda x: list(map(lambda y: (x, y), lista2)), lista1))

print (listaNueva)
```

## Ejercicio 13

### Pipe de transformaciones de listas

Implementa una función que tome una lista de funciones y una lista de números, y aplique cada función de la lista en secuencia sobre la lista de números usando reduce.

```python
# Ejemplo de funciones y lista de entrada
funciones = [lambda x: x*2, lambda x: x+3, lambda x: x-1]
numeros = [1, 2, 3]
# Resultado esperado: [((1*2)+3)-1, ((2*2)+3)-1, ((3*2)+3)-1] -> [4, 6, 8]
```

Ejercicio:

```python
from functools import reduce
funciones = [lambda x: x*2, lambda x: x+3, lambda x: x-1]
numeros = [1, 2, 3]

lista = reduce(lambda acc, f: [f(x) for x in acc], funciones, numeros)

print(lista)
```

## Ejercicio 14

### Aplicar operaciones de cadena

Dada una lista de cadenas, usa map y filter para crear una nueva lista con las cadenas que tengan más de tres letras y en las que todas las letras sean mayúsculas. Además, convierte el primer carácter en minúscula.

```python
# Ejemplo de lista de entrada
palabras = ["HOLA", "MUNDO", "SOL", "CIELO", "mar"]
# Resultado esperado: ['hOLA', 'mUNDO', 'cIELO']
```

Ejercicio:

```python
palabras = ["HOLA", "MUNDO", "SOL", "CIELO", "mar", "arena"]

palabras = list(filter(lambda x: len(x) > 3, palabras))

palabras = list(filter(lambda x: x.isupper(), palabras))

palabras = list(map(lambda x: x[0].lower() + x[1::], palabras))

print(palabras)
```